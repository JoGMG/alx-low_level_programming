#include <stdio.h>
#include "function_pointers.h"

/**
 * main - prints name
 * @name - name of 
 */

void print_name(char *name, void (*f)(char *))
{
	if(name != NULL) && (name != NULL)

		f(name);
}
